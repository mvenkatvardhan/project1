{
  "name": "nodejs-cicd-demo",
  "version": "1.0.0",
  "description": "Demo Node.js app for Jenkins CI/CD pipeline with SonarQube, OWASP Dependency-Check and Trivy",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --coverage --coverageReporters=lcov --coverageReporters=text",
    "lint": "eslint src test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
